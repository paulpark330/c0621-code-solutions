/* exported getFirstChar */
//  New storage for first character of the string.
//  Put the first character of the string in the new storage
//  Give back the new storage

function getFirstChar(string) {
  var firstChar = '';
  firstChar = string[0];
  return firstChar;
}
