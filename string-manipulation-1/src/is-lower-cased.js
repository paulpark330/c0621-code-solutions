/* exported isLowerCased */
//  IF word is lowercased
//    RETURN true
//  ELSE RETURN false
//

function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}
