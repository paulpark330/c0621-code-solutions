/* exported isUpperCased */
//  IF word is uppercased
//    RETURN true
//  ELSE return false
//

function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}
