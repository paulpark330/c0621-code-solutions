import React from 'react';
import ReactDOM from 'react-dom';
import ToggleSwitch from './toggle-switch';

const element = (
  <ToggleSwitch text="HI" />
);

ReactDOM.render(element, document.querySelector('#root'));
